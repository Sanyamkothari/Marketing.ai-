"""Test fixtures package: synthetic data generation and hand-written broken config files.

Nothing in here is real data. `make_data.py` produces **synthetic** CSVs for tests and local
development only; they never reach the UI as if they were a customer's data (plan §13.3).
"""

from __future__ import annotations
