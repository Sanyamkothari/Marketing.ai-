"""India DPDP engineering controls (Phase 4b M48): consent, retention, erasure and access requests.

Engineering controls that support compliance; they are not legal advice, and Minfy's legal or
compliance team must review them before a client relies on them. The modules, in the order a reader
meets them: `config` (configs/privacy.yaml), `tables` (the three SQL tables), `consent` (the ledger
and the scoring seam), `layout` (where personal data lives in the artefact store), `rewrite` (how a
file is searched and rewritten in its own format), `retention`, `lifecycle`, `erasure` and
`access_export`.
"""
