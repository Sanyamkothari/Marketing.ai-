"""Data onboarding: clients, sources, column mappings and the datasets they produce (Phase 2).

Owned by the `phase-2-onboarding` branch. The contracts-first task created this package and
`specs.py` so the import path exists and the ownership is recorded; everything in it is Phase 2's
to write.
"""
