"""Generative and hybrid features: RAG, root-cause summaries and generated copy (Phase 3a).

Owned by the `phase-3a-generative` branch. The contracts-first task created this package and
`contracts.py`; the `LLMClient` protocol and `FakeLLMClient` those features are written against
live in `engine/llm.py`, where Phase 3a adds `BedrockLLMClient` beside them.
"""
