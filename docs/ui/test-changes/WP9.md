# WP9 test changes (prototype part)

Every assertion WP9 changed in `tests/prototype/*.mjs`, and why. Each one follows a deliberate change of
markup, layout or wording in `marketing-ai-prototype.html` (Revision 6 in `CHANGELOG-prototype.md`).
No behavioural assertion was loosened: every number, sentence from the engine, id and data attribute the
tests read is still asserted. One product-source pin was widened; it is marked below.

| File | Test | Old assertion | New assertion | Why |
|---|---|---|---|---|
| `usecases.test.mjs` | `the client selector sits in the header of every screen` (renamed `… in the top bar on every per-client screen`) | `.headtools #f-client` on `#/`, `#/uc/rca`, `#/uc/rca/data`, `#/uc/win-back-campaign/output`; `.chint` text | `#pb-bar .tb-context #f-client` on `#/uc/rca`, `#/uplift/win-back-campaign`, `#/pilot/kit`, `#/pilot`; the hint is the `.clientpick .tt-pop` toggletip; never in `#app .head`; absent on `#/`, a Data page and an Output page | The owner's logo-only header: the chooser moved to the top bar, shown only on per-client routes (plan navigation), and the `.chint` hint became a toggletip. Value, options and "+ New client" unchanged. |
| `existing.test.mjs` | `the lifecycle overview is unchanged` | `.h1` = "Marketing AI", `.sub` = "Customer Lifecycle", `.hint` = "Select a use case to see its Data → Model → Output pipeline." | `.crumbs` = "Home"; `.h1` = "Customer Lifecycle"; `.desc` starts "Pick what you want to predict or improve."; the colour toggletip names the three AI types; no `.legend`, no per-stage type chip; a `.foryou` line | Home follows the one header pattern (breadcrumb root Home, H1 the journey, one sentence), one toggletip replaces the legend and stage chips, and a "For you" line is added (plan WP1/top 5). Stage pills and use-case links still pinned. |
| `consistency.test.mjs` | `every new panel has a mobile layout` | `".headtools{order:-1;width:100%;"` | `".tb-row{padding:0 16px;gap:8px;flex-wrap:wrap}"` | The header tools block is gone; the chooser's mobile layout is now the top bar's one row (wordmark, chooser, Menu). |
| `consistency.test.mjs` | `the uplift label, defaults and thresholds are the product's` | `$(dom, ".uentry a")` | `$(dom, ".head-actions a.related")` | The injected pill became the quiet related link in the header actions. Same href, same follow-through. |
| `consistency.test.mjs` | `the uplift entry point and its words are ui/modules/uplift's (DEC-608)` | `index.js` contains the pill markup; `.uentry` innerHTML = `Uplift for <b>this use case</b> ›<span>…</span>` | `index.js` contains either the pre-v1 pill or "Also: target with uplift" (**widened product-source pin**); no `.uentry`; `.head-actions a.related` outerHTML = `<a class="related" href="#/uplift/win-back-campaign" title="Uplift predicts …">Also: target with uplift ›</a>` | The audit's navigation section replaces the pill with "Also: target with uplift ›". WP5 rewrites `index.js` in parallel, so the pin accepts both until it merges; the integrator should pin the WP5 markup exactly once it lands. `UPLIFT_EXPLANATION` and the route are still pinned to `views.js`. |
| `uplift.test.mjs` | `DEC-608: Phase 1's change… never offers Uplift, …` | `$(dom, ".uentry a")` | `$(dom, ".head-actions a.related")` | As above. |
| `uplift.test.mjs` | `Results → Output after scoring: …` | `#f-campaign` exists "outside the three blocks"; `.flow .block` length 3 | `.flow .block#f-campaign` exists; `.flow .block` length 4 | Plan navigation: "Campaign results for this run" becomes the fourth flow block of a scoring run. |
| `uplift.test.mjs` | `Campaign results: a list scored today says when …` | `Object.values(kpiMap)` = four "—" | two "—" | Campaign results lead with a verdict and two labelled tiles; lift and incremental conversions moved into Statistical details (plan top 5, item 3). |
| `uplift.test.mjs` | `Campaign results: the 1 May campaign, measured …` | `kpiMap` = {Treated "11,200 · 11.0%", Control "1,450 · 7.52%", Lift "+3.48 pts", Incremental conversions "390"} | `.verdict .vline` = "The campaign worked: about 390 extra customers came back."; `kpiMap` = {"Contacted customers who came back": "11.0% of 11,200", "Control group who came back": "7.5% of 1,450"} | As above; whole people and 1-decimal rates (design rules, numbers). Every kv row (absolute lift with CI, relative lift, incremental conversions with CI, p-value, …) and the engine summary are still asserted unchanged. |
| `uplift.test.mjs` | `Campaign results: an uploaded outcomes file is not measured …` | four "—"; after the sample, `kpiMap.Lift` = "+3.48 pts" | two "—"; after the sample, `kv("Absolute lift")` = "+3.48 pts (95% CI 1.91 to 4.86)" | The lift tile moved into Statistical details; the same number (with its interval) is asserted there. |
| `uplift.test.mjs` | `Campaign results for a run with a 0% control group …` | `kpiMap.Lift` = "+3.48 pts" for r0501 | `kv("Absolute lift")` = "+3.48 pts (95% CI 1.91 to 4.86)" | As above. |
| `uplift.test.mjs` | `the plain win-back sample is the Phase 1 path …` | `.uentry a` href | `.head-actions a.related` href | Related link, as above. |
| `uplift.test.mjs` | `DEC-608: win-back's Setup links to its uplift screen …` | `.uentry` after the `.rule` with `aria-label="Uplift"`, pill innerHTML and explanation span, on Setup, Running, Results and the four run pages; the Overview's "Uplift modelling ›" pill; `.back` href on the uplift screen | the related link in `.head .head-actions` ("Also: target with uplift ›", the explanation as its title, href `#/uplift/win-back-campaign`) on the same states and pages; no `.uentry`; Home has no pill and the top bar's Models menu has "Uplift models" → `#/uplift`; the uplift screen's breadcrumb is Home › Customer Lifecycle › Win-back Campaign › Uplift with the use-case link back | Plan navigation: pills leave the page body; every "‹ back" link becomes the Home-rooted breadcrumb. Presence on every state, absence on the other six use cases, title, H1, description, chips, mode labels and the index are unchanged. |
| `uplift.test.mjs` | `DEC-608: the uplift screen has its own form …` | crumbs = ["Customer Lifecycle", "Win-back Campaign", "Uplift", page] | crumbs = ["Home", "Customer Lifecycle", "Win-back Campaign", "Uplift", page] | The breadcrumb root is always Home. |

## New tests

| File | Test |
|---|---|
| `usecases.test.mjs` | `one top bar: the six goals, the active one following the route, and the logo alone in the header` (six items in order, active goal per route incl. Campaign results → Campaigns, header = titles + logo only, the sample-data chip, a menu opening with `aria-expanded` and closing on Escape) |
