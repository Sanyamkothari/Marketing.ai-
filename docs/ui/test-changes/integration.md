# Test assertions changed at integration

| File | Test | Old assertion | New assertion | Why |
|---|---|---|---|---|
| tests/integration/test_acceptance.py:339 | the Phase 1 browser journey | `get_by_role("heading", name="Marketing AI")` | `get_by_role("heading", name="Customer Lifecycle")` | Home's H1 is now the journey the user is looking at; the product name moved to the top bar's wordmark (WP1). Same check: the journey returns to Home. |
| tests/prototype/consistency.test.mjs | the uplift entry point and its words are ui/modules/uplift's (DEC-608) | (WP9, temporary) `index.includes("Also: target with uplift") \|\| index.includes(<old pill>)` | `index.includes('<a class="related" href="${esc(routes.setup(uc.id))}">Also: target with uplift ›</a>')` | Re-pins the product's exact markup now that WP5 has landed; WP9's either-or was a stop-gap while both were in flight. This is stricter than the pre-v1 pin, not looser. The retired pill markup (`retiredEntryHtml`) that only this pin kept alive is removed. |
| tests/integration/test_onboarding_acceptance.py:345 | the raw-tables browser journey | `get_by_role("heading", name="Marketing AI")` | `get_by_role("heading", name="Customer Lifecycle")` | Same reason as test_acceptance.py:339: Home's H1 is the journey; the product name is the top bar's wordmark (WP1). |
| tests/integration/test_onboarding_acceptance.py | (capture for) test_use_this_dataset_filled_step_two_from_the_manifest | `page.locator(".ptype .pill").inner_text()` | `.text_content()` | WP2 moved the kind of prediction into the folded Advanced settings; `inner_text` of a hidden element is empty. The assertion (it starts with "classification") is unchanged. |
| tests/integration/test_onboarding_acceptance.py | test_the_training_run_read_the_built_dataset | `value.endswith("(built)")`; `"key <pk>" in meta and "target <label>" in meta` | `value == "Built dataset"`; `"ID <pk>" in meta and "predicting <label>" in meta` | WP2's Results data block names the source in words (the dataset id moved to Technical details) and says "ID … · predicting …". Still proves the run read the built dataset keyed on both columns. |
| tests/integration/uplift/test_uplift_browser.py | (fixture) the uplift journey | `.tab.on` contains "Output" | `.tab.on` contains "Contact list" | WP5 names the uplift Output step by what it holds, the contact list. |
| tests/integration/uplift/test_uplift_browser.py | (capture helper) `capture` / new `disclose` | rows read with `inner_text` of every `.kv` as the page first appears | text, tiles and screenshot still taken as the page first appears; then each named `<details>` is asserted to be closed, opened by clicking its summary, and the `.kv` rows are read; the opened text is kept as `Screen.disclosed` | WP5 moved the technical rows behind closed disclosures, whose content renders only once open (`inner_text` is empty). Keeping the text as first seen keeps "AUUC / p-value not in the visible text" meaningful. |
| tests/integration/uplift/test_uplift_browser.py | test_the_model_page_shows_auuc_with_its_interval_from_the_artefact | `model.rows["AUUC"]`, `["Hold-out rows"]`, `["Bootstrap resamples"]` read with the page as first shown | the same rows, same 3-decimal tolerance and same artefact values, read after opening "Technical metrics" and "Training setup" | AUUC is in "Technical metrics"; hold-out rows and resamples are in "Training setup" (WP5). |
| tests/integration/uplift/test_uplift_browser.py | test_the_off_policy_estimate_is_the_stored_report | `screen.rows["Policy"]`, `["Rows"]` read with the page as first shown | the same equality checks against `ope_report.json`, read after opening "How this was estimated" | The OPE rows sit in the "How this was estimated" disclosure under the plain result (WP5). |
| tests/integration/uplift/test_uplift_browser.py | test_the_output_page_shows_segments_and_the_recommendation; test_the_score_output_is_the_treat_list_the_browser_downloaded (via `check_output`) | `rows["Expected incremental conversions"]` and `rows["Cost per contact"]` read with the page as first shown | the same checks (value and 95% interval at 0 places vs `policy_recommendation.json`; cost per contact is "—"), read after opening "More about this recommendation" on both the training and the scoring Contact list | The interval row and the empty cost row moved into "More about this recommendation" (WP5). |
| tests/integration/uplift/test_uplift_browser.py | test_both_score_screens_offer_the_uplift_model | uplift score Setup: `#u-model option` read with `all_inner_texts()` | `all_text_contents()` (the Phase 1 dropdown still uses `all_inner_texts()`); assertion unchanged | With one model, WP5 shows its name and folds the dropdown under "Change model"; `inner_text` of a hidden option is empty. |
| tests/integration/uplift/test_uplift_browser.py | test_a_mature_campaign_shows_the_lift_with_its_interval | `rows["Absolute lift"]`, `["Outcome window"]`, `["Judged as of"]` read with the page as first shown | the same checks (lift and its interval at 1 place vs `incrementality_report.json`; "90 days"; "15 Aug 2026, 11:59 pm"), read after opening "Statistical details" and "How it was measured" | The statistics and the measurement settings sit in those two disclosures (WP5). |
| tests/integration/uplift/test_uplift_browser.py | test_no_screen_rendered_a_placeholder_for_a_value | no `undefined`/`NaN`/`null`/`Infinity`/`[object Object]` in `screen.text` | the same pattern in `screen.text` and in `screen.disclosed` | Stricter: the numbers that moved behind Details are still checked for placeholders. |
