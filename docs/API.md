# API and artefact reference

Generated by `python -m scripts.gen_api_docs`. Do not edit by hand: run the command and commit the result.

Contract schema version: 1.

## Endpoints

| Method | Path | Summary | Response model |
|---|---|---|---|
| GET | `/healthz` | Liveness probe | HealthResponse |
| GET | `/industries` | Every industry journey with its stages and use-case cards | IndustriesResponse |
| GET | `/use-cases/{use_case_id}` | One merged use-case configuration, its Setup copy and its advanced-settings schema | UseCaseResponse |
| GET | `/use-cases/{use_case_id}/template.csv` | The upload template of one use case as CSV | - |
| GET | `/use-cases/{use_case_id}/template_README.md` | The upload template's README of one use case as Markdown | - |

## Run artefacts

One file per stage in `data/runs/<run_id>/`. Every field below is what the UI renders.

A training run writes: `baseline.json`, `best_model.json`, `confusion_matrix.json`, `decile_lift.json`, `drift_baseline.json`, `evaluation.json`, `fairness.json`, `feature_importance.json`, `leaderboard.json`, `model/`, `prepare.json`, `profile.json`, `row_explanations.parquet`, `run.json`, `run_config.json`, `run_manifest.json`, `schema.json`, `split.json`, `status.json`, `validation.json`.

A scoring run writes: `drift.json`, `prepare.json`, `profile.json`, `row_explanations.parquet`, `run.json`, `run_config.json`, `run_manifest.json`, `scores.csv`, `scores.parquet`, `scoring_summary.json`, `status.json`, `validation.json`.

### `run.json`

`run.json` - the Results summary bar, the three pipeline blocks and Previous runs.

| Field | Type | Required | Meaning |
|---|---|---|---|
| `schema_version` | integer | no | Version of the contract the file was written with. |
| `run_id` | string | yes | Sortable run id, also the run directory name and the seed source. |
| `use_case_id` | string | yes | Id of the use case that was run. |
| `use_case_name` | string | yes | Display name, so run history renders without loading configs. |
| `mode` | RunMode ("train" \| "score") | yes | Whether this run trained a model or scored new data. |
| `state` | RunState ("pending" \| "running" \| "done" \| "failed" \| "cancelled" \| "skipped") | yes | Overall run state: pending, running, done, failed or cancelled. |
| `created_at` | datetime (ISO-8601, with timezone) | yes | UTC time the run record was created. |
| `started_at` | datetime (ISO-8601, with timezone) \| null | no | UTC time the first stage started. |
| `finished_at` | datetime (ISO-8601, with timezone) \| null | no | UTC time the run reached a final state. |
| `upload_id` | string | yes | Id of the upload this run consumed. |
| `file_name` | string | yes | The user's original file name, shown in the Results bar and run history. |
| `row_count` | integer \| null | no | Rows in the upload, taken from the dataset profile. |
| `primary_key` | string | yes | Column identifying each entity. |
| `target` | string \| null | no | Target column; set for training runs only. |
| `problem_type` | ProblemType ("binary_classification" \| "regression" \| "forecasting" \| "clustering") | yes | Problem type resolved for this run, detected or overridden. |
| `model_choice` | string | yes | Step-3 choice: the AutoML sentinel or a single model family value. |
| `model_version_id` | string \| null | no | Model version produced (train) or used (score). |
| `best_model` | string \| null | no | Display name of the winning or scoring model. |
| `headline_metric` | Metric ("roc_auc" \| "pr_auc" \| "f1" \| "recall" \| "precision" \| "rmse" \| "mae") \| null | no | Metric behind the headline score. |
| `headline_metric_label` | string \| null | no | Catalog label of the headline metric. |
| `headline_score` | number \| null | no | Test score of the headline metric, rounded. |
| `champion` | boolean | no | Whether this run's model is, or became, the champion. |
| `beat_previous_champion` | boolean | no | Whether this run's model beat the previous champion. |
| `overrides` | object | no | Expanded overrides the user sent, in config units. |
| `artefacts` | object of string -> string | no | Artefact filename mapped to its storage key. |
| `error` | RunError \| null | no | Failure detail; set when the state is failed. |
| `engine_version` | string | yes | Version of the engine package that produced the run. |

#### RunError

The failure attached to a failed run or stage.

| Field | Type | Required | Meaning |
|---|---|---|---|
| `schema_version` | integer | no | Version of the contract the file was written with. |
| `code` | string | yes | Machine-readable failure code, from the validation table or the engine. |
| `message` | string | yes | Business-language explanation of what went wrong. |
| `stage` | StageKey ("ingest" \| "validate" \| "prepare" \| "split" \| "train" \| "evaluate" \| "explain" \| "register" \| "validate_against_schema" \| "predict" \| "explain_rows" \| "actions" \| "export") \| null | yes | Stage that failed, when one was running. |

### `status.json`

`status.json` - everything the Running screen polls.

| Field | Type | Required | Meaning |
|---|---|---|---|
| `schema_version` | integer | no | Version of the contract the file was written with. |
| `run_id` | string | yes | Run this status belongs to. |
| `mode` | RunMode ("train" \| "score") | yes | Whether the run is training or scoring. |
| `state` | RunState ("pending" \| "running" \| "done" \| "failed" \| "cancelled" \| "skipped") | yes | Overall run state, mirroring the run record. |
| `updated_at` | datetime (ISO-8601, with timezone) | yes | UTC time this status file was last written. |
| `stages` | list[StageStatus] | yes | The full stage list in execution order, eight for train and seven for score. |
| `current_stage` | StageKey ("ingest" \| "validate" \| "prepare" \| "split" \| "train" \| "evaluate" \| "explain" \| "register" \| "validate_against_schema" \| "predict" \| "explain_rows" \| "actions" \| "export") \| null | no | Stage currently running, when any. |
| `progress_pct` | integer | yes | Pre-computed completion percentage, done stages over total. |

#### StageStatus

One engine stage on the Running screen (the UI groups rows by `group_label`; DEC-020).

| Field | Type | Required | Meaning |
|---|---|---|---|
| `schema_version` | integer | no | Version of the contract the file was written with. |
| `key` | StageKey ("ingest" \| "validate" \| "prepare" \| "split" \| "train" \| "evaluate" \| "explain" \| "register" \| "validate_against_schema" \| "predict" \| "explain_rows" \| "actions" \| "export") | yes | Engine stage id (plan sections 6.1 and 6.2). |
| `title` | string | yes | Engine label for logs and docs; the UI never shows it. |
| `group_label` | string | yes | Prototype Running-screen line this stage belongs to. |
| `state` | RunState ("pending" \| "running" \| "done" \| "failed" \| "cancelled" \| "skipped") | yes | Stage state: pending, running, done, failed, cancelled or skipped. |
| `detail` | string | no | Second line under the row, pre-formatted by the stage. |
| `started_at` | datetime (ISO-8601, with timezone) \| null | no | UTC time this stage started. |
| `ended_at` | datetime (ISO-8601, with timezone) \| null | no | UTC time this stage ended. |
| `duration_seconds` | number \| null | no | Wall-clock duration of the stage. |
| `error` | RunError \| null | no | Failure detail when this stage failed. |

### `run_config.json`

Serialised verbatim as `run_config.json` (plan section 5).

| Field | Type | Required | Meaning |
|---|---|---|---|
| `schema_version` | integer | yes |  |
| `use_case_id` | string | yes |  |
| `resolved_at` | datetime (ISO-8601, with timezone) | yes |  |
| `config` | UseCaseConfig | yes | A fully merged, validated use case. This is what the whole engine consumes. |
| `overrides_applied` | object | yes |  |
| `sources` | object of string -> "engine" \| "use_case" \| "override" \| "derived" | yes |  |
| `warnings` | list[string] | no |  |

#### UseCaseConfig

A fully merged, validated use case. This is what the whole engine consumes.

| Field | Type | Required | Meaning |
|---|---|---|---|
| `id` | string | yes |  |
| `name` | string | yes |  |
| `description` | string | no |  |
| `lifecycle_stage` | string | yes |  |
| `ai_type` | AiType ("predictive" \| "generative" \| "hybrid") | no |  |
| `problem_type` | ProblemType ("binary_classification" \| "regression" \| "forecasting" \| "clustering") | no |  |
| `entity` | string | no |  |
| `target` | TargetConfig | no |  |
| `primary_key_hints` | list[string] | no |  |
| `time_column_hints` | list[string] | no |  |
| `validation` | ValidationConfig | no |  |
| `prepare` | PrepareConfig | no |  |
| `split` | SplitConfig | no |  |
| `features` | FeaturesConfig | no |  |
| `model_search` | ModelSearchConfig | no |  |
| `evaluation` | EvaluationConfig | no |  |
| `actions` | ActionsConfig | no |  |
| `monitoring` | MonitoringConfig | no |  |
| `governance` | GovernanceConfig | no |  |
| `output` | OutputConfig | yes |  |
| `ui` | UiConfig | yes |  |
| `template` | TemplateConfig | no |  |

#### TargetConfig

| Field | Type | Required | Meaning |
|---|---|---|---|
| `column` | string \| null | no |  |
| `positive_label` | string \| integer \| boolean \| null | no |  |
| `definition` | string | no |  |
| `label_source` | string | no |  |

#### ValidationConfig

| Field | Type | Required | Meaning |
|---|---|---|---|
| `min_rows` | integer | no |  |
| `min_positive` | integer | no |  |
| `max_file_size_mb` | integer | no |  |
| `imbalance_warn_min_rate` | number | no |  |
| `imbalance_warn_max_rate` | number | no |  |
| `high_null_column_rate` | number | no |  |
| `leakage_check` | boolean | no |  |
| `leakage_auc_threshold` | number | no |  |
| `acknowledged` | list[string] | no |  |

#### PrepareConfig

| Field | Type | Required | Meaning |
|---|---|---|---|
| `missing_values` | MissingValues ("auto" \| "fill" \| "drop_rows") | no |  |
| `outliers` | Outliers ("clip" \| "remove_rows" \| "keep") | no |  |
| `pii_handling` | PiiHandling ("redact" \| "drop_columns" \| "keep") | no |  |
| `deduplicate` | boolean | no |  |
| `exclude_columns` | list[string] | no |  |

#### SplitConfig

| Field | Type | Required | Meaning |
|---|---|---|---|
| `type` | SplitType ("random_stratified" \| "time_based") | no |  |
| `validation_fraction` | number | no |  |
| `test_fraction` | number | no |  |
| `time_column` | string \| null | no |  |
| `group_column` | string \| null | no |  |

#### FeaturesConfig

| Field | Type | Required | Meaning |
|---|---|---|---|
| `auto_feature_engineering` | boolean | no |  |
| `categorical_encoding` | CategoricalEncoding ("auto" \| "one_hot" \| "target" \| "ordinal") | no |  |
| `numeric_scaling` | NumericScaling ("auto" \| "standard" \| "min_max" \| "none") | no |  |
| `text_columns` | TextHandling ("ignore" \| "tfidf" \| "embeddings") | no |  |
| `selection` | FeatureSelection ("importance" \| "correlation_filter" \| "pca" \| "none") | no |  |
| `max_features` | integer | no |  |

#### ModelSearchConfig

| Field | Type | Required | Meaning |
|---|---|---|---|
| `metric` | Metric ("roc_auc" \| "pr_auc" \| "f1" \| "recall" \| "precision" \| "rmse" \| "mae") | no |  |
| `metric_choices` | list[Metric ("roc_auc" \| "pr_auc" \| "f1" \| "recall" \| "precision" \| "rmse" \| "mae")] | no |  |
| `strategy` | Strategy ("fast" \| "balanced" \| "exhaustive") | no |  |
| `candidate_pool` | list[ModelFamily ("XGBoost" \| "LightGBM" \| "RandomForest" \| "LogisticRegression" \| "CatBoost" \| "NeuralNet")] | no |  |
| `candidates` | list[ModelFamily ("XGBoost" \| "LightGBM" \| "RandomForest" \| "LogisticRegression" \| "CatBoost" \| "NeuralNet")] | no |  |
| `ensemble` | boolean | no |  |
| `tuning_trials` | integer | no |  |
| `time_limit_minutes` | integer | no |  |
| `folds` | integer | no |  |
| `imbalance` | Imbalance ("auto" \| "class_weights" \| "oversampling" \| "none") | no |  |

#### EvaluationConfig

| Field | Type | Required | Meaning |
|---|---|---|---|
| `calibration` | Calibration ("isotonic" \| "platt" \| "none") | no |  |
| `threshold` | ThresholdConfig | no |  |
| `shap` | boolean | no |  |
| `reasons_per_row` | integer | no |  |
| `fairness_column` | string \| null | no |  |
| `champion_min_improvement_pct` | number | no |  |

#### ActionsConfig

| Field | Type | Required | Meaning |
|---|---|---|---|
| `score_field` | string | no |  |
| `bands` | list[Band] | no |  |
| `suppression` | SuppressionConfig | no |  |
| `control_group_fraction` | number | no |  |

#### MonitoringConfig

| Field | Type | Required | Meaning |
|---|---|---|---|
| `drift_psi_threshold` | number | no |  |
| `retraining` | Retraining ("manual" \| "on_drift" \| "weekly" \| "monthly") | no |  |
| `performance_alert_drop_pct` | integer | no |  |

#### GovernanceConfig

| Field | Type | Required | Meaning |
|---|---|---|---|
| `retention_days` | integer | no |  |
| `consent_column` | string \| null | no |  |
| `approval_required` | boolean | no |  |

#### OutputConfig

| Field | Type | Required | Meaning |
|---|---|---|---|
| `kpi` | KpiConfig | yes |  |

#### UiConfig

| Field | Type | Required | Meaning |
|---|---|---|---|
| `target_label` | string | no |  |
| `mode_labels` | ModeCopy | yes |  |
| `mode_help` | ModeCopy | yes |  |
| `dataset_hint` | ModeCopy | yes |  |
| `columns_hint` | ModeCopy | yes |  |
| `run_button` | ModeCopy | yes |  |
| `pages` | PageTitles | yes |  |

#### TemplateConfig

| Field | Type | Required | Meaning |
|---|---|---|---|
| `columns` | list[TemplateColumn] | no |  |

#### ThresholdConfig

| Field | Type | Required | Meaning |
|---|---|---|---|
| `mode` | ThresholdMode ("auto" \| "fixed" \| "manual") | no |  |
| `value` | number | no |  |

#### Band

| Field | Type | Required | Meaning |
|---|---|---|---|
| `name` | string | yes |  |
| `min_score` | number | yes |  |
| `action` | string | yes |  |

#### SuppressionConfig

| Field | Type | Required | Meaning |
|---|---|---|---|
| `suppress_opted_out` | boolean | no |  |
| `opt_out_column` | string \| null | no |  |
| `suppress_recently_contacted` | boolean | no |  |
| `recently_contacted_column` | string \| null | no |  |
| `recently_contacted_days` | integer | no |  |

#### KpiConfig

| Field | Type | Required | Meaning |
|---|---|---|---|
| `label` | string | yes |  |
| `formula` | string | yes |  |

#### ModeCopy

| Field | Type | Required | Meaning |
|---|---|---|---|
| `train` | string | yes |  |
| `score` | string | yes |  |

#### PageTitles

| Field | Type | Required | Meaning |
|---|---|---|---|
| `data` | string | yes |  |
| `model` | string | yes |  |
| `output` | string | yes |  |

#### TemplateColumn

| Field | Type | Required | Meaning |
|---|---|---|---|
| `name` | string | yes |  |
| `role` | ColumnRole ("primary_key" \| "time" \| "feature" \| "target" \| "consent" \| "contact") | yes |  |
| `type` | ColumnType ("string" \| "integer" \| "float" \| "boolean" \| "date" \| "datetime" \| "text") | yes |  |
| `description` | string | yes |  |
| `examples` | tuple[string, string, string, string, string] | yes |  |

### `profile.json`

`profile.json` - the Setup preview and the Data page.

| Field | Type | Required | Meaning |
|---|---|---|---|
| `schema_version` | integer | no | Version of the contract the file was written with. |
| `upload_id` | string | yes | Id of the upload that was profiled. |
| `file_name` | string | yes | The user's original file name. |
| `file_size_bytes` | integer | yes | Size of the uploaded file in bytes. |
| `file_format` | "csv" \| "parquet" | yes | Format the file was read as. |
| `delimiter` | string \| null | yes | Delimiter sniffed for CSV; null for Parquet. |
| `encoding` | string | yes | Character encoding the file was decoded with. |
| `row_count` | integer | yes | Number of data rows in the file. |
| `column_count` | integer | yes | Number of columns in the file. |
| `columns` | list[ColumnProfile] | yes | One profile per column, in file order. |
| `primary_key_candidates` | list[string] | yes | Unique, non-null columns, hint-name matches first. |
| `time_column_candidates` | list[string] | yes | Columns that look like time columns and parse as dates. |
| `target_candidate` | string \| null | yes | Column matching the use case's configured target column. |
| `preview_rows` | list[list[string]] | yes | First five rows, stringified, for the Setup preview table. |
| `missing_value_rate_pct` | number | yes | Share of missing cells over the whole table, as a percentage. |
| `fingerprint` | DatasetFingerprint | yes | Identity of this exact dataset, for comparing runs against each other. |
| `profiled_at` | datetime (ISO-8601, with timezone) | yes | UTC time the file was profiled. |

#### ColumnProfile

Everything ingest learned about one column of the uploaded file.

| Field | Type | Required | Meaning |
|---|---|---|---|
| `schema_version` | integer | no | Version of the contract the file was written with. |
| `name` | string | yes | Column name exactly as it appears in the file header. |
| `position` | integer | yes | Zero-based position of the column in the file. |
| `dtype` | string | yes | Pandas or Arrow dtype as read, for example int64. |
| `inferred_type` | ColumnType ("string" \| "integer" \| "float" \| "boolean" \| "date" \| "datetime" \| "text") | yes | Engine-level type inferred for this column. |
| `null_count` | integer | yes | Number of null or empty values. |
| `null_rate` | number | yes | Share of rows that are null, 0 to 1. |
| `distinct_count` | integer | yes | Number of distinct non-null values. |
| `is_unique` | boolean | yes | Whether every non-null value is distinct. |
| `is_constant` | boolean | yes | Whether the column holds a single distinct value. |
| `sample_values` | list[string] | yes | Up to five stringified example values; never written to the log. |
| `minimum` | number \| null | no | Smallest value; numeric columns only. |
| `maximum` | number \| null | no | Largest value; numeric columns only. |
| `mean` | number \| null | no | Arithmetic mean; numeric columns only. |
| `top_categories` | list[CategoryCount] | no | Ten most frequent values; categorical columns only. |
| `looks_like_id` | boolean | yes | Distinct count is close to the row count and this is not the primary key. |
| `looks_like_time` | boolean | yes | Name matches the time-like pattern or values parse as dates. |
| `pii_kinds` | list[string] | no | Names of the PII detectors that matched; never the matched values. |

#### DatasetFingerprint

Identifies the exact dataset a run consumed (DEC-042).  Saved at ingest and carried on every run, so a later reader can tell whether two runs saw the same data. Without it, comparing two runs' metrics is guesswork: a score only means something next to the data that produced it.

| Field | Type | Required | Meaning |
|---|---|---|---|
| `schema_version` | integer | no | Version of the contract the file was written with. |
| `hash` | string | yes | Hex digest of the dataset content and shape. |
| `algorithm` | string | yes | Digest algorithm, so the scheme can change without ambiguity. |
| `n_rows` | integer | yes | Number of data rows covered by the digest. |
| `columns` | list[string] | yes | Column names, in file order, covered by the digest. |

#### CategoryCount

One category of a categorical column and how often it occurs.

| Field | Type | Required | Meaning |
|---|---|---|---|
| `schema_version` | integer | no | Version of the contract the file was written with. |
| `value` | string | yes | The category value, stringified. |
| `count` | integer | yes | Number of rows holding this value. |
| `share` | number | yes | Share of non-null rows holding this value, 0 to 1. |

### `validation.json`

`validation.json` - every check that ran, errors first.

| Field | Type | Required | Meaning |
|---|---|---|---|
| `schema_version` | integer | no | Version of the contract the file was written with. |
| `run_id` | string \| null | no | Run id, or null when an upload is validated alone. |
| `upload_id` | string | yes | Upload the checks ran against. |
| `mode` | RunMode ("train" \| "score") | yes | Whether the checks are the train or the score set. |
| `checks` | list[ValidationCheck] | yes | All checks: errors, then warnings, then info, in validation-table order. |
| `error_count` | integer | yes | Number of blocking errors, excluding acknowledged ones. |
| `warning_count` | integer | yes | Number of warnings. |
| `passed` | boolean | yes | True when no blocking error remains, so a run may start. |
| `validated_at` | datetime (ISO-8601, with timezone) | yes | UTC time the checks were run. |

#### ValidationCheck

One row of the validation table, already interpolated for the user.

| Field | Type | Required | Meaning |
|---|---|---|---|
| `schema_version` | integer | no | Version of the contract the file was written with. |
| `code` | string | yes | Validation table code, for example PK_NOT_UNIQUE. |
| `severity` | Severity ("error" \| "warning" \| "info") | yes | Whether this check blocks the run or is only reported. |
| `message` | string | yes | Business-language message, with the numbers already filled in. |
| `suggestion` | string | no | What the user can do about it. |
| `column` | string \| null | no | Column the check is about, when it is about one. |
| `details` | object | no | Machine-readable numbers behind the message. |
| `acknowledgeable` | boolean | no | Whether the UI may offer to acknowledge this check. |
| `acknowledged` | boolean | no | Whether the user acknowledged it through the run overrides. |

### `prepare.json`

`prepare.json` - what preparation changed, so scoring can repeat it.

| Field | Type | Required | Meaning |
|---|---|---|---|
| `schema_version` | integer | no | Version of the contract the file was written with. |
| `run_id` | string | yes | Run this report belongs to. |
| `rows_in` | integer | yes | Rows read into the prepare stage. |
| `rows_out` | integer | yes | Rows left after preparation. |
| `columns_in` | integer | yes | Columns read into the prepare stage. |
| `columns_out` | integer | yes | Columns left after preparation. |
| `feature_columns` | list[string] | yes | Exact ordered feature list handed to training. |
| `dropped_columns` | list[DroppedColumn] | yes | Columns dropped, with reasons. |
| `row_removals` | list[RowRemoval] | yes | Row removals grouped by reason. |
| `transforms` | list[Transform] | yes | Transforms in replay order. |
| `pii_columns` | list[string] | no | Columns the PII detectors matched. |
| `consent_column` | string \| null | no | Consent column applied, when configured. |
| `consent_rows_removed` | integer | no | Rows removed because consent was not given. |
| `detail` | string | yes | Pre-formatted Running-screen line for the preparation step. |
| `prepared_at` | datetime (ISO-8601, with timezone) | yes | UTC time preparation finished. |

#### DroppedColumn

One column removed before training, and why.

| Field | Type | Required | Meaning |
|---|---|---|---|
| `schema_version` | integer | no | Version of the contract the file was written with. |
| `name` | string | yes | Name of the dropped column. |
| `reason` | "user_excluded" \| "constant" \| "high_null" \| "id_like" \| "pii" \| "leakage" | yes | Why the column was dropped. |
| `detail` | string | no | One line of extra context, for example the null rate. |

#### RowRemoval

Rows removed by one prepare rule.

| Field | Type | Required | Meaning |
|---|---|---|---|
| `schema_version` | integer | no | Version of the contract the file was written with. |
| `reason` | "duplicate" \| "consent_false" \| "missing_target" \| "outlier" | yes | Why these rows were removed. |
| `rows` | integer | yes | Number of rows removed for this reason. |

#### Transform

One transform fitted on the training data and replayed at score time.

| Field | Type | Required | Meaning |
|---|---|---|---|
| `schema_version` | integer | no | Version of the contract the file was written with. |
| `order` | integer | yes | Position of this transform in the replay order. |
| `kind` | "fill_median" \| "fill_mode" \| "clip_percentile" \| "redact" \| "cast" \| "dedupe" \| "consent_filter" | yes | Kind of transform applied. |
| `columns` | list[string] | yes | Columns the transform was applied to. |
| `parameters` | object of string -> number \| string \| boolean | no | Fitted parameters, so scoring replays the transform identically. |

### `split.json`

`split.json` - how the data was divided into train, validation and test.

| Field | Type | Required | Meaning |
|---|---|---|---|
| `schema_version` | integer | no | Version of the contract the file was written with. |
| `run_id` | string | yes | Run this report belongs to. |
| `type` | SplitType ("random_stratified" \| "time_based") | yes | Split strategy that was used. |
| `time_column` | string \| null | yes | Column ordering a time-based split. |
| `group_column` | string \| null | yes | Column whose groups are kept in one part. |
| `parts` | list[SplitPart] | yes | Train, validation and test, in that order. |
| `validation_fraction` | number | yes | Configured validation share, as a fraction. |
| `test_fraction` | number | yes | Configured test share, as a fraction. |
| `train_cutoff` | datetime (ISO-8601, with timezone) \| null | no | Last training timestamp; time-based only. |
| `test_cutoff` | datetime (ISO-8601, with timezone) \| null | no | First test timestamp; time-based only. |
| `seed` | integer | yes | Random seed derived from the run id, so the split is reproducible. |
| `detail` | string | yes | Pre-formatted Running-screen line for the split step. |
| `split_at` | datetime (ISO-8601, with timezone) | yes | UTC time the split was made. |

#### SplitPart

One part of the split and its shape.

| Field | Type | Required | Meaning |
|---|---|---|---|
| `schema_version` | integer | no | Version of the contract the file was written with. |
| `name` | "train" \| "validation" \| "test" | yes | Which part of the split this is. |
| `rows` | integer | yes | Rows in this part. |
| `share` | number | yes | Share of the prepared rows in this part, 0 to 1. |
| `positive_rows` | integer \| null | no | Positive rows; classification only. |
| `positive_rate` | number \| null | no | Positive rate 0 to 1; classification only. |
| `start_date` | datetime (ISO-8601, with timezone) \| null | no | Earliest timestamp; time-based only. |
| `end_date` | datetime (ISO-8601, with timezone) \| null | no | Latest timestamp; time-based only. |

### `leaderboard.json`

`leaderboard.json` - every model AutoGluon trained, best first.

| Field | Type | Required | Meaning |
|---|---|---|---|
| `schema_version` | integer | no | Version of the contract the file was written with. |
| `run_id` | string | yes | Run this leaderboard belongs to. |
| `metric` | Metric ("roc_auc" \| "pr_auc" \| "f1" \| "recall" \| "precision" \| "rmse" \| "mae") | yes | Metric every score is measured in. |
| `metric_label` | string | yes | Catalog label of the metric. |
| `greater_is_better` | boolean | yes | Whether a higher score is a better score. |
| `entries` | list[LeaderboardEntry] | yes | Leaderboard rows, sorted by rank. |
| `best_model_name` | string | yes | AutoGluon name of the winning model. |
| `models_trained` | integer | yes | How many models were trained. |
| `time_limit_seconds` | integer | yes | Training time limit that applied to the search. |
| `presets` | string | yes | AutoGluon presets the search strategy mapped to. |

#### LeaderboardEntry

One trained model on the leaderboard.

| Field | Type | Required | Meaning |
|---|---|---|---|
| `schema_version` | integer | no | Version of the contract the file was written with. |
| `rank` | integer | yes | Rank on the validation score, one is best. |
| `model_name` | string | yes | AutoGluon model name, for example LightGBM_BAG_L1. |
| `family` | ModelFamily ("XGBoost" \| "LightGBM" \| "RandomForest" \| "LogisticRegression" \| "CatBoost" \| "NeuralNet") \| null | yes | Engine family; null for the ensemble. |
| `family_label` | string \| null | yes | Catalog label of the family, for the Model page. |
| `validation_score` | number | yes | Score on the validation split, in the chosen metric. |
| `test_score` | number \| null | yes | Score on the test split, when computed. |
| `fit_time_seconds` | number | yes | Seconds spent fitting this model. |
| `predict_time_seconds` | number | yes | Seconds spent predicting the validation split. |
| `is_ensemble` | boolean | yes | Whether this entry is a weighted ensemble of other models. |
| `stack_level` | integer | yes | Stacking level the model was trained at. |

### `best_model.json`

`best_model.json` - the winning model, ready for the Results summary bar.

| Field | Type | Required | Meaning |
|---|---|---|---|
| `schema_version` | integer | no | Version of the contract the file was written with. |
| `run_id` | string | yes | Run this model belongs to. |
| `model_name` | string | yes | AutoGluon name of the winning model. |
| `family` | ModelFamily ("XGBoost" \| "LightGBM" \| "RandomForest" \| "LogisticRegression" \| "CatBoost" \| "NeuralNet") \| null | yes | Engine family; null for the ensemble. |
| `is_ensemble` | boolean | yes | Whether the winner is an ensemble. |
| `ensemble_members` | list[string] | no | Member model names of the ensemble. |
| `display_name` | string | yes | Name shown to the user, for example Ensemble (XGBoost + LightGBM). |
| `metric` | Metric ("roc_auc" \| "pr_auc" \| "f1" \| "recall" \| "precision" \| "rmse" \| "mae") | yes | Metric the scores below are measured in. |
| `metric_label` | string | yes | Catalog label of the metric. |
| `validation_score` | number | yes | Score on the validation split. |
| `test_score` | number | yes | Score on the test split, the headline number. |
| `hyperparameters` | object | no | Hyperparameters as reported by AutoGluon. |
| `hyperparameters_summary` | string | yes | One-line hyperparameter summary for the Model page. |
| `training_rows` | integer | yes | Rows the model was fitted on. |
| `feature_count` | integer | yes | Number of features the model was fitted on. |
| `fit_time_seconds` | number | yes | Seconds spent fitting the winning model. |
| `predictor_key` | string | yes | Storage key of the saved AutoGluon predictor directory. |
| `trained_at` | datetime (ISO-8601, with timezone) | yes | UTC time training finished. |

### `evaluation.json`

`evaluation.json` - the test-split verdict (plan section 6.3, evaluate).

| Field | Type | Required | Meaning |
|---|---|---|---|
| `schema_version` | integer | no | Version of the contract the file was written with. |
| `run_id` | string | yes | Run this evaluation belongs to. |
| `problem_type` | ProblemType ("binary_classification" \| "regression" \| "forecasting" \| "clustering") | yes | Problem type the metrics belong to. |
| `rows_evaluated` | integer | yes | Rows in the test split. |
| `positive_rate` | number \| null | yes | Positive rate of the test split, 0 to 1. |
| `primary_metric` | Metric ("roc_auc" \| "pr_auc" \| "f1" \| "recall" \| "precision" \| "rmse" \| "mae") | yes | Metric the model was optimised for. |
| `primary_metric_label` | string | yes | Catalog label of the primary metric. |
| `headline_score` | number | yes | Primary metric on the test split, the headline number. |
| `metrics` | list[MetricValue] | yes | Every catalog metric valid for this problem type, in catalog order. |
| `extra_metrics` | object of string -> number | no | Named scores outside the metric enum, such as r2 or accuracy. |
| `threshold` | number | yes | Decision threshold the classification metrics were computed at. |
| `threshold_mode` | ThresholdMode ("auto" \| "fixed" \| "manual") | yes | How the threshold was chosen. |
| `threshold_detail` | string | yes | One line explaining the threshold, for the Model page. |
| `calibration` | CalibrationSummary \| null | yes | Calibration summary; null when calibration was off. |
| `evaluated_at` | datetime (ISO-8601, with timezone) | yes | UTC time evaluation finished. |

#### MetricValue

One evaluated metric, ready to render.

| Field | Type | Required | Meaning |
|---|---|---|---|
| `schema_version` | integer | no | Version of the contract the file was written with. |
| `id` | Metric ("roc_auc" \| "pr_auc" \| "f1" \| "recall" \| "precision" \| "rmse" \| "mae") | yes | Metric id from the catalog. |
| `label` | string | yes | Catalog label of the metric. |
| `value` | number | yes | Value on the test split, rounded. |
| `greater_is_better` | boolean | yes | Whether a higher value is a better value. |

#### CalibrationSummary

How probabilities were calibrated and what it bought.

| Field | Type | Required | Meaning |
|---|---|---|---|
| `schema_version` | integer | no | Version of the contract the file was written with. |
| `method` | Calibration ("isotonic" \| "platt" \| "none") | yes | Calibration method applied. |
| `brier_before` | number \| null | yes | Brier score before calibration. |
| `brier_after` | number \| null | yes | Brier score after calibration. |
| `fitted_on` | "validation" | no | Split the calibrator was fitted on. |

### `confusion_matrix.json`

`confusion_matrix.json` - the four-cell grid the Model page draws.

| Field | Type | Required | Meaning |
|---|---|---|---|
| `schema_version` | integer | no | Version of the contract the file was written with. |
| `run_id` | string | yes | Run this matrix belongs to. |
| `threshold` | number | yes | Decision threshold the matrix was computed at. |
| `positive_label` | string | yes | Label shown for the positive class. |
| `negative_label` | string | yes | Label shown for the negative class. |
| `true_positive` | integer | yes | Positives predicted positive. |
| `false_negative` | integer | yes | Positives predicted negative. |
| `false_positive` | integer | yes | Negatives predicted positive. |
| `true_negative` | integer | yes | Negatives predicted negative. |
| `total` | integer | yes | Rows in the matrix. |
| `cells` | tuple[integer, integer, integer, integer] | yes | The grid in render order: true positive, false negative, false positive, true negative. |
| `precision` | number | yes | Precision at this threshold. |
| `recall` | number | yes | Recall at this threshold. |
| `specificity` | number | yes | Specificity at this threshold. |
| `f1` | number | yes | F1 score at this threshold. |

### `decile_lift.json`

`decile_lift.json` - the Output page's decile chart, pre-computed.

| Field | Type | Required | Meaning |
|---|---|---|---|
| `schema_version` | integer | no | Version of the contract the file was written with. |
| `run_id` | string | yes | Run this table belongs to. |
| `mode` | "classification" \| "regression" | yes | Which family of numbers the bins hold. |
| `base_rate_pct` | number \| null | yes | Overall positive rate as a percentage; classification only. |
| `bins` | list[DecileBin] | yes | Exactly ten bins, highest-scoring decile first. |
| `values` | list[number] | yes | The ten numbers the bar chart draws, highest decile first. |
| `unit` | "x" \| "%" | yes | Unit the drawn values are in. |
| `caption` | string | yes | Caption under the chart. |
| `computed_at` | datetime (ISO-8601, with timezone) | yes | UTC time the table was computed. |

#### DecileBin

One decile of the score distribution.

| Field | Type | Required | Meaning |
|---|---|---|---|
| `schema_version` | integer | no | Version of the contract the file was written with. |
| `decile` | integer | yes | Decile number, one holds the highest scores. |
| `label` | string | yes | Label of the decile, D1 to D10. |
| `rows` | integer | yes | Rows in this decile. |
| `score_min` | number | yes | Lowest score in this decile. |
| `score_max` | number | yes | Highest score in this decile. |
| `mean_score` | number | yes | Mean score in this decile. |
| `positives` | integer \| null | no | Actual positives; classification only. |
| `actual_rate_pct` | number \| null | no | Actual positive rate as a percentage; classification only. |
| `actual_mean` | number \| null | no | Mean actual value; regression only. |
| `predicted_mean` | number \| null | no | Mean predicted value; regression only. |
| `lift` | number \| null | no | Lift of this decile over the base rate. |
| `cumulative_lift` | number \| null | no | Lift of this decile and the ones above. |
| `cumulative_capture_pct` | number \| null | no | Share of all positives captured down to this decile, as a percentage. |

### `baseline.json`

`baseline.json` - the separately trained baseline the Model page compares against.

| Field | Type | Required | Meaning |
|---|---|---|---|
| `schema_version` | integer | no | Version of the contract the file was written with. |
| `run_id` | string | yes | Run this comparison belongs to. |
| `baseline_name` | string | yes | Display name of the baseline model. |
| `baseline_family` | ModelFamily ("XGBoost" \| "LightGBM" \| "RandomForest" \| "LogisticRegression" \| "CatBoost" \| "NeuralNet") | yes | Engine family the baseline was trained with. |
| `rows` | list[BaselineMetric] | yes | One row per line of the comparison table. |
| `model_beats_baseline` | boolean | yes | Whether the model wins on the primary metric. |

#### BaselineMetric

One line of the model-versus-baseline table.

| Field | Type | Required | Meaning |
|---|---|---|---|
| `schema_version` | integer | no | Version of the contract the file was written with. |
| `id` | Metric ("roc_auc" \| "pr_auc" \| "f1" \| "recall" \| "precision" \| "rmse" \| "mae") | yes | Metric id from the catalog. |
| `label` | string | yes | Catalog label of the metric. |
| `model_value` | number | yes | The trained model's value. |
| `baseline_value` | number \| null | yes | The baseline model's value. |
| `delta` | number \| null | yes | Model value minus baseline value. |
| `model_better` | boolean \| null | yes | Whether the model beats the baseline here. |

### `fairness.json`

`fairness.json` - reported only, never blocking.

| Field | Type | Required | Meaning |
|---|---|---|---|
| `schema_version` | integer | no | Version of the contract the file was written with. |
| `run_id` | string | yes | Run this report belongs to. |
| `column` | string \| null | yes | Sensitive column, when one was configured. |
| `evaluated` | boolean | yes | Whether the fairness check actually ran. |
| `reason_not_evaluated` | string \| null | yes | Why the check did not run. |
| `groups` | list[FairnessGroup] | yes | Groups, largest first. |
| `max_positive_rate_gap` | number \| null | yes | Largest positive-rate gap. |
| `max_recall_gap` | number \| null | yes | Largest recall gap. |
| `max_precision_gap` | number \| null | yes | Largest precision gap. |
| `note` | string | no | Standing note shown with the table. |

#### FairnessGroup

One group of the sensitive column.

| Field | Type | Required | Meaning |
|---|---|---|---|
| `schema_version` | integer | no | Version of the contract the file was written with. |
| `value` | string | yes | Value of the sensitive column identifying this group. |
| `rows` | integer | yes | Rows in this group. |
| `positive_rate` | number | yes | Predicted positive rate in this group, 0 to 1. |
| `recall` | number \| null | yes | Recall within this group. |
| `precision` | number \| null | yes | Precision within this group. |

### `feature_importance.json`

`feature_importance.json` - the global explanation of the model.

| Field | Type | Required | Meaning |
|---|---|---|---|
| `schema_version` | integer | no | Version of the contract the file was written with. |
| `run_id` | string | yes | Run this explanation belongs to. |
| `method` | "permutation" \| "shap" | yes | How importance was computed. |
| `computed_on` | "test" | no | Split importance was computed on. |
| `top_n` | integer | yes | How many features were kept, at most twenty. |
| `items` | list[FeatureImportanceItem] | yes | Features, most important first. |
| `caption` | string | yes | Caption under the chart, naming the method. |

#### FeatureImportanceItem

One feature on the global importance chart.

| Field | Type | Required | Meaning |
|---|---|---|---|
| `schema_version` | integer | no | Version of the contract the file was written with. |
| `rank` | integer | yes | Rank by importance, one is most important. |
| `feature` | string | yes | Feature name. |
| `importance` | number | yes | Raw importance as reported by the method. |
| `share_pct` | number | yes | Importance as a percentage, normalised over the returned rows. |
| `stddev` | number \| null | no | Standard deviation of the importance estimate. |
| `p_value` | number \| null | no | P-value of the permutation importance estimate. |

### `drift_baseline.json`

`drift_baseline.json` - the training distributions scoring runs compare against.

| Field | Type | Required | Meaning |
|---|---|---|---|
| `schema_version` | integer | no | Version of the contract the file was written with. |
| `run_id` | string | yes | Training run that produced this baseline. |
| `model_version_id` | string | yes | Model version this baseline was stored with. |
| `rows` | integer | yes | Training rows the baseline was computed from. |
| `bin_count` | integer | yes | Number of histogram bins used for numeric features. |
| `features` | list[FeatureBaseline] | yes | One entry per feature, in fit order. |
| `created_at` | datetime (ISO-8601, with timezone) | yes | UTC time the baseline was stored. |

#### FeatureBaseline

The training distribution of one feature, kept for drift comparison.

| Field | Type | Required | Meaning |
|---|---|---|---|
| `schema_version` | integer | no | Version of the contract the file was written with. |
| `feature` | string | yes | Feature name. |
| `kind` | "numeric" \| "categorical" | yes | Which kind of summary is stored. |
| `null_rate` | number | yes | Share of nulls at training time, 0 to 1. |
| `bins` | list[HistogramBin] | no | Histogram bins; numeric features only. |
| `categories` | list[CategoryCount] | no | Frequency table; categorical features only. |
| `mean` | number \| null | no | Training mean; numeric features only. |
| `std` | number \| null | no | Training standard deviation; numeric features only. |

#### HistogramBin

One bin of a numeric feature's training distribution.

| Field | Type | Required | Meaning |
|---|---|---|---|
| `schema_version` | integer | no | Version of the contract the file was written with. |
| `lower` | number | yes | Lower edge of the bin. |
| `upper` | number | yes | Upper edge of the bin. |
| `count` | integer | yes | Training rows in this bin. |
| `share` | number | yes | Share of training rows in this bin, 0 to 1. |

### `drift.json`

`drift.json` - how far the scored file has moved from the training data.

| Field | Type | Required | Meaning |
|---|---|---|---|
| `schema_version` | integer | no | Version of the contract the file was written with. |
| `run_id` | string | yes | Scoring run this report belongs to. |
| `baseline_run_id` | string | yes | Training run whose baseline was used. |
| `model_version_id` | string | yes | Model version that was scored with. |
| `threshold` | number | yes | Configured drift threshold; half of it is the watch level. |
| `features` | list[FeatureDrift] | yes | Features, most drifted first. |
| `max_psi` | number | yes | Largest population stability index over all features. |
| `drifted_features` | list[string] | yes | Features at or above the threshold. |
| `status` | DriftStatus ("stable" \| "watch" \| "drifted") | yes | Overall verdict for the scoring run. |
| `summary` | string | yes | One-line summary for the Output page. |
| `computed_at` | datetime (ISO-8601, with timezone) | yes | UTC time drift was computed. |

#### FeatureDrift

Drift of one feature between the training data and the scored file.

| Field | Type | Required | Meaning |
|---|---|---|---|
| `schema_version` | integer | no | Version of the contract the file was written with. |
| `feature` | string | yes | Feature name. |
| `psi` | number | yes | Population stability index against the training baseline. |
| `status` | DriftStatus ("stable" \| "watch" \| "drifted") | yes | Verdict for this feature. |
| `null_rate_baseline` | number | yes | Null share at training time, 0 to 1. |
| `null_rate_current` | number | yes | Null share in the scored file, 0 to 1. |

### `scoring_summary.json`

`scoring_summary.json` - everything the Output page needs without reading the scores file.

| Field | Type | Required | Meaning |
|---|---|---|---|
| `schema_version` | integer | no | Version of the contract the file was written with. |
| `run_id` | string | yes | Scoring run this summary belongs to. |
| `model_version_id` | string | yes | Model version the rows were scored with. |
| `model_display_name` | string | yes | Display name of that model. |
| `rows_scored` | integer | yes | Rows that were scored. |
| `score_field` | string | yes | Name of the score column in the exported files. |
| `score_mean` | number | yes | Mean score over the scored rows. |
| `score_median` | number | yes | Median score over the scored rows. |
| `bands` | list[BandCount] | yes | Band counts, in configured band order. |
| `actions` | list[ActionCount] | yes | Action counts, largest first. |
| `suppressed` | list[SuppressionCount] | yes | Suppression counts by reason. |
| `control_group_rows` | integer | yes | Rows held out as the control group. |
| `kpi` | KpiValue | yes | The configured headline KPI. |
| `drift_status` | DriftStatus ("stable" \| "watch" \| "drifted") \| null | no | Drift verdict, when drift was computed. |
| `drift_max_psi` | number \| null | no | Largest population stability index. |
| `drift_summary` | string \| null | no | One-line drift summary. |
| `files` | object of string -> string | yes | Exported filename mapped to its storage key. |
| `sample_rows` | list[ScoreRow] | yes | First ten scored rows, so the page renders without the export. |
| `scored_at` | datetime (ISO-8601, with timezone) | yes | UTC time scoring finished. |

#### BandCount

How many scored rows fell into one band.

| Field | Type | Required | Meaning |
|---|---|---|---|
| `schema_version` | integer | no | Version of the contract the file was written with. |
| `name` | string | yes | Band name from the configuration. |
| `action` | string | yes | Action configured for the band. |
| `min_score` | number | yes | Lowest score that still falls into this band. |
| `rows` | integer | yes | Rows in this band. |
| `share_pct` | number | yes | Share of scored rows in this band, as a percentage. |

#### ActionCount

How many scored rows received one action.

| Field | Type | Required | Meaning |
|---|---|---|---|
| `schema_version` | integer | no | Version of the contract the file was written with. |
| `action` | string | yes | Action assigned to the rows. |
| `rows` | integer | yes | Rows that received it. |
| `share_pct` | number | yes | Share of scored rows with this action, as a percentage. |

#### SuppressionCount

How many rows one suppression rule removed from targeting.

| Field | Type | Required | Meaning |
|---|---|---|---|
| `schema_version` | integer | no | Version of the contract the file was written with. |
| `reason` | "opted_out" \| "recently_contacted" \| "consent_false" | yes | Why the rows were suppressed. |
| `rows` | integer | yes | Rows suppressed for this reason. |

#### KpiValue

The headline KPI tile of the Output page, computed from the configured formula.

| Field | Type | Required | Meaning |
|---|---|---|---|
| `schema_version` | integer | no | Version of the contract the file was written with. |
| `label` | string | yes | KPI label from the configuration. |
| `formula` | string | yes | KPI formula from the configuration, as written. |
| `value` | number | yes | Computed value of the formula. |
| `display` | string | yes | Pre-formatted value, for example 184K. |

#### ScoreRow

Row schema of `scores.csv` and `scores.parquet`.

| Field | Type | Required | Meaning |
|---|---|---|---|
| `schema_version` | integer | no | Version of the contract the file was written with. |
| `primary_key` | string | yes | Primary-key value, so scores join back to the source system. |
| `score` | number | yes | Model score for this row. |
| `band` | string | yes | Band the score falls into. |
| `action` | string | yes | Action assigned to the row. |
| `reasons` | list[Reason] | yes | Top reasons behind the score. |
| `suppressed_reason` | string \| null | no | Why the row was suppressed, when it was. |
| `control_group` | boolean | no | Whether the row was held out as a control. |

#### Reason

One per-row reason behind a score.

| Field | Type | Required | Meaning |
|---|---|---|---|
| `schema_version` | integer | no | Version of the contract the file was written with. |
| `feature` | string | yes | Feature the reason is about. |
| `value` | string | yes | The row's value for that feature, stringified. |
| `contribution` | number | yes | Signed contribution of this feature to the score. |
| `direction` | Direction ("up" \| "down") | yes | Whether the feature pushed the score up or down. |
| `text` | string | yes | Ready-to-render sentence for this reason. |

### `schema.json`

`schema.json` (plan section 4.4) - saved with every model and checked at score time.

| Field | Type | Required | Meaning |
|---|---|---|---|
| `schema_version` | integer | no | Version of the contract the file was written with. |
| `use_case_id` | string | yes | Use case the schema belongs to. |
| `model_version_id` | string | yes | Model version the schema was saved with. |
| `primary_key` | string | yes | Primary-key column. |
| `target` | string \| null | yes | Target column; null for scoring-only schemas. |
| `problem_type` | ProblemType ("binary_classification" \| "regression" \| "forecasting" \| "clustering") | yes | Problem type the model was fitted for. |
| `columns` | list[FeatureSchemaColumn] | yes | Columns in the exact order used at fit time. |
| `row_count_at_fit` | integer | yes | Rows the model was fitted on. |
| `created_at` | datetime (ISO-8601, with timezone) | yes | UTC time the schema was written. |

#### FeatureSchemaColumn

One column of the schema a scoring file must match.

| Field | Type | Required | Meaning |
|---|---|---|---|
| `schema_version` | integer | no | Version of the contract the file was written with. |
| `name` | string | yes | Column name as used at fit time. |
| `inferred_type` | ColumnType ("string" \| "integer" \| "float" \| "boolean" \| "date" \| "datetime" \| "text") | yes | Engine-level type of the column. |
| `required` | boolean | no | Whether a scoring file must contain the column. |
| `nullable` | boolean | no | Whether nulls are accepted in the column. |
| `categories` | list[string] | no | Categories seen at fit time, when few enough. |
| `minimum` | number \| null | no | Smallest value seen at fit time. |
| `maximum` | number \| null | no | Largest value seen at fit time. |

### `run_manifest.json`

`run_manifest.json` - one flat record per run, written for every run.

| Field | Type | Required | Meaning |
|---|---|---|---|
| `schema_version` | integer | no | Version of the contract the file was written with. |
| `run_id` | string | yes | Run this manifest describes. |
| `recipe` | Recipe \| null | no | Training choices; null for a scoring run that did not fit a model. |
| `dataset_fingerprint` | DatasetFingerprint | yes | Identity of the data the run consumed. |
| `seed` | integer | yes | Seed that made the run reproducible. |
| `metrics` | object of string -> number | no | Headline numbers, metric id to value; empty when the run produced none. |
| `leaderboard_path` | string \| null | no | Storage key of leaderboard.json; null when no search was run. |
| `duration_s` | number | yes | Wall-clock seconds from run start to final state. |
| `cost_estimate` | CostEstimate | yes | What the run cost to produce. |
| `created_at` | datetime (ISO-8601, with timezone) | yes | UTC time the manifest was written. |

#### Recipe

Every choice that determines a trained model, and nothing else.  `train(recipe)` is the only entry point to training, so this object is the complete description of a training attempt. The defining property is:      the same `Recipe` applied to the same `DatasetFingerprint`     must produce the same model.  That is what makes a training run reproducible, and what lets a later layer reason about which recipes work without re-reading the whole configuration.  Evaluation settings are deliberately ABSENT. How a model is measured is not a training choice, and the evaluate stage takes only the model, the test data and the evaluation config, so that any stored model can be re-scored on demand (DEC-043, DEC-044). Actions, monitoring and governance are absent for the same reason: they shape what happens to predictions, not how the model is fitted.

| Field | Type | Required | Meaning |
|---|---|---|---|
| `use_case_id` | string | yes | Use case this recipe belongs to. |
| `problem_type` | ProblemType ("binary_classification" \| "regression" \| "forecasting" \| "clustering") | yes | Learning task the model is fitted for. |
| `target` | string | yes | Column the model learns to predict. |
| `primary_key` | string | yes | Row identifier; never used as a feature. |
| `feature_columns` | list[string] | yes | Exact ordered feature list handed to training, after exclusions. |
| `prepare` | PrepareConfig | yes | Cleaning and exclusion choices applied before fitting. |
| `split` | SplitConfig | yes | How rows are partitioned into train, validation and test. |
| `features` | FeaturesConfig | yes | Encoding, scaling and feature-selection choices. |
| `model_search` | ModelSearchConfig | yes | Metric, strategy, candidate families and budget. |
| `seed` | integer | yes | Seed for every stochastic step, derived from the run id. |

#### CostEstimate

What a run cost to produce.  `estimated_usd` is null for a local run rather than zero: nothing was billed, and a fabricated zero would be indistinguishable from a real measurement of free compute (plan section 13.3).

| Field | Type | Required | Meaning |
|---|---|---|---|
| `schema_version` | integer | no | Version of the contract the file was written with. |
| `compute_seconds` | number | yes | Wall-clock seconds of compute the run consumed. |
| `estimated_usd` | number \| null | no | Billed cost when the platform reports one; null when nothing was billed. |
| `basis` | string | yes | How the estimate was derived, in plain words. |

## Tabular artefacts

Row-oriented files. The model below describes ONE ROW of the file.

### `row_explanations.parquet`

Row schema of `row_explanations.parquet` - the top reasons per scored row.

| Field | Type | Required | Meaning |
|---|---|---|---|
| `schema_version` | integer | no | Version of the contract the file was written with. |
| `primary_key` | string | yes | Primary-key value of the explained row. |
| `score` | number | yes | Score the reasons explain. |
| `reasons` | list[Reason] | yes | Top reasons, strongest contribution first. |

### `scores.csv`

Row schema of `scores.csv` and `scores.parquet`.

| Field | Type | Required | Meaning |
|---|---|---|---|
| `schema_version` | integer | no | Version of the contract the file was written with. |
| `primary_key` | string | yes | Primary-key value, so scores join back to the source system. |
| `score` | number | yes | Model score for this row. |
| `band` | string | yes | Band the score falls into. |
| `action` | string | yes | Action assigned to the row. |
| `reasons` | list[Reason] | yes | Top reasons behind the score. |
| `suppressed_reason` | string \| null | no | Why the row was suppressed, when it was. |
| `control_group` | boolean | no | Whether the row was held out as a control. |

### `scores.parquet`

Row schema of `scores.csv` and `scores.parquet`.

| Field | Type | Required | Meaning |
|---|---|---|---|
| `schema_version` | integer | no | Version of the contract the file was written with. |
| `primary_key` | string | yes | Primary-key value, so scores join back to the source system. |
| `score` | number | yes | Model score for this row. |
| `band` | string | yes | Band the score falls into. |
| `action` | string | yes | Action assigned to the row. |
| `reasons` | list[Reason] | yes | Top reasons behind the score. |
| `suppressed_reason` | string \| null | no | Why the row was suppressed, when it was. |
| `control_group` | boolean | no | Whether the row was held out as a control. |

### `scores.csv` header rule

`engine.contracts.scores_csv_columns(config, primary_key)` builds the header:

```
Header of `scores.csv` / `scores.parquet`, in order.

`(<primary_key>, <actions.score_field>, "band", "action", "reason_1".."reason_n",
"suppressed_reason", "control_group")`, where `n` is `evaluation.reasons_per_row`.
```

## Registry record

Stored in SQLite by `engine.registry`, not in a run directory.

### `ModelVersion`

The registry record of one trained model (SQLite, not a run artefact).

| Field | Type | Required | Meaning |
|---|---|---|---|
| `schema_version` | integer | no | Version of the contract the file was written with. |
| `model_id` | string | yes | Unique id of this model version. |
| `use_case_id` | string | yes | Use case the model belongs to. |
| `version` | integer | yes | Version number within the use case, starting at one. |
| `run_id` | string | yes | Training run that produced the model. |
| `created_at` | datetime (ISO-8601, with timezone) | yes | UTC time the version was registered. |
| `status` | ModelStatus ("candidate" \| "pending_approval" \| "champion" \| "archived") | yes | Lifecycle status of the version. |
| `metric` | Metric ("roc_auc" \| "pr_auc" \| "f1" \| "recall" \| "precision" \| "rmse" \| "mae") | yes | Metric the scores below are measured in. |
| `metric_label` | string | yes | Catalog label of the metric. |
| `test_score` | number | yes | Test-split score, the number the champion rule compares. |
| `validation_score` | number \| null | no | Validation-split score. |
| `model_display_name` | string | yes | Display name of the model. |
| `schema_key` | string | yes | Storage key of the saved feature schema. |
| `run_config_key` | string | yes | Storage key of the resolved run configuration. |
| `predictor_key` | string | yes | Storage key of the saved predictor directory. |
| `drift_baseline_key` | string \| null | no | Storage key of the drift baseline. |
| `artefact_keys` | object of string -> string | no | Artefact filename mapped to its storage key. |
| `approved_by` | string \| null | no | Who approved the version. |
| `approved_at` | datetime (ISO-8601, with timezone) \| null | no | UTC time of the approval. |
| `promoted_at` | datetime (ISO-8601, with timezone) \| null | no | UTC time the version became champion. |
| `promoted_by` | string \| null | no | Who promoted the version. |
| `promotion_note` | string \| null | no | Why the version was promoted. |
| `previous_champion_id` | string \| null | no | Version this one replaced as champion. |
| `improvement_pct` | number \| null | no | Percentage improvement over the previous champion. |
| `engine_version` | string | yes | Engine version that trained the model. |
| `autogluon_version` | string | yes | AutoGluon version that trained the model. |

The saved predictor lives in the `model/` directory of the run; it has no JSON contract.

## Configuration reference

Advanced settings in the UI are exactly the fields of the use-case schema. Values are in
config units: fractions stay fractions, and `scale` is the factor the UI multiplies by for display.

### 1. Data preparation

| Path | Label | Widget | Default | Min | Max | Step | Scale |
|---|---|---|---|---|---|---|---|
| `prepare.missing_values` | Missing values | select | auto | - | - | - | - |
| `prepare.outliers` | Outliers | select | clip | - | - | - | - |
| `prepare.pii_handling` | PII handling | select | redact | - | - | - | - |
| `validation.min_positive` | Min positive examples | number | 200 | 50 | 100000 | 50 | - |
| `prepare.deduplicate` | Remove duplicate rows | checkbox | true | - | - | - | - |
| `validation.leakage_check` | Check for target leakage | checkbox | true | - | - | - | - |
| `prepare.exclude_columns` | Exclude columns from features | column-multi-select | (empty) | - | - | - | - |

### 2. Data split

| Path | Label | Widget | Default | Min | Max | Step | Scale |
|---|---|---|---|---|---|---|---|
| `split.type` | Split type | select | random_stratified | - | - | - | - |
| `split.validation_fraction` | Validation (%) | number | 0.15 | 0.05 | 0.4 | 0.05 | 100 |
| `split.test_fraction` | Test (%) | number | 0.15 | 0.05 | 0.4 | 0.05 | 100 |
| `split.time_column` | Time column | column-select | null | - | - | - | - |
| `split.group_column` | Group column (keep together) | column-select | null | - | - | - | - |

### 3. Feature engineering

| Path | Label | Widget | Default | Min | Max | Step | Scale |
|---|---|---|---|---|---|---|---|
| `features.auto_feature_engineering` | Automatic feature engineering (dates, ratios, aggregates) | checkbox | true | - | - | - | - |
| `features.categorical_encoding` | Categorical encoding | select | auto | - | - | - | - |
| `features.numeric_scaling` | Numeric scaling | select | auto | - | - | - | - |
| `features.text_columns` | Text columns | select | ignore | - | - | - | - |
| `features.selection` | Feature selection | select | importance | - | - | - | - |
| `features.max_features` | Max features | number | 100 | 10 | 500 | 10 | - |

### 4. Model search

| Path | Label | Widget | Default | Min | Max | Step | Scale |
|---|---|---|---|---|---|---|---|
| `model_search.candidates` |  | multi-select | XGBoost, LightGBM, RandomForest, LogisticRegression | - | - | - | - |
| `model_search.strategy` | Search strategy | select | balanced | - | - | - | - |
| `model_search.tuning_trials` | Tuning trials | number | 50 | 5 | 500 | 10 | - |
| `model_search.time_limit_minutes` | Time limit (min) | number | 30 | 1 | 240 | 1 | - |
| `model_search.folds` | CV folds | number | 5 | 2 | 10 | 1 | - |
| `model_search.imbalance` | Class imbalance | select | auto | - | - | - | - |
| `model_search.ensemble` | Ensemble / stack the best models | checkbox | true | - | - | - | - |

### 5. Evaluation & explainability

| Path | Label | Widget | Default | Min | Max | Step | Scale |
|---|---|---|---|---|---|---|---|
| `model_search.metric` | Optimise for | select | roc_auc | - | - | - | - |
| `evaluation.calibration` | Probability calibration | select | isotonic | - | - | - | - |
| `evaluation.threshold.mode` | Decision threshold | select | auto | - | - | - | - |
| `evaluation.threshold.value` | Threshold value | number | 0.5 | 0.01 | 0.99 | 0.01 | - |
| `evaluation.reasons_per_row` | Reasons per row | number | 3 | 1 | 5 | 1 | - |
| `evaluation.fairness_column` | Fairness check (sensitive column) | column-select | null | - | - | - | - |
| `evaluation.champion_min_improvement_pct` | Replace champion if better by (%) | number | 1.0 | 0 | 20 | 0.5 | - |
| `evaluation.shap` | Generate SHAP explanations per row | checkbox | true | - | - | - | - |

### 6. Actions & output

| Path | Label | Widget | Default | Min | Max | Step | Scale |
|---|---|---|---|---|---|---|---|
| `actions.bands[0].min_score` | High-risk score ≥ | number | 0.8 | 0 | 1 | 0.05 | - |
| `actions.bands[1].min_score` | Medium-risk score ≥ | number | 0.5 | 0 | 1 | 0.05 | - |
| `actions.control_group_fraction` | Control group holdout (%) | number | 0.1 | 0 | 0.5 | 0.01 | 100 |
| `actions.suppression.recently_contacted_days` | Recently contacted (days) | number | 14 | 1 | 90 | 1 | - |
| `actions.suppression.suppress_opted_out` | Suppress opted-out customers | checkbox | true | - | - | - | - |
| `actions.suppression.suppress_recently_contacted` | Suppress recently contacted | checkbox | true | - | - | - | - |

### 7. Monitoring & retraining

| Path | Label | Widget | Default | Min | Max | Step | Scale |
|---|---|---|---|---|---|---|---|
| `monitoring.drift_psi_threshold` | Drift alert (PSI above) | number | 0.2 | 0.05 | 1 | 0.05 | - |
| `monitoring.retraining` | Retraining | select | on_drift | - | - | - | - |
| `monitoring.performance_alert_drop_pct` | Performance alert (% drop) | number | 5 | 1 | 50 | 1 | - |

### 8. Governance & privacy

| Path | Label | Widget | Default | Min | Max | Step | Scale |
|---|---|---|---|---|---|---|---|
| `governance.retention_days` | Data retention (days) | number | 90 | 0 | 730 | 30 | - |
| `governance.consent_column` | Consent column (use rows where true) | column-select | null | - | - | - | - |
| `governance.approval_required` | Require approval before a model becomes champion | checkbox | true | - | - | - | - |

### Other `configs/engine.yaml` keys

Keys of the default document that no advanced-settings field renders, with their file comments.

| Key | Notes |
|---|---|
| `schema_version` | int; bumped on any breaking change of this document's shape |
| `ai_type` | enum: predictive \| generative \| hybrid |
| `problem_type` | enum: catalog.problem_types ids; forecasting/clustering load but are not trainable; a run override re-derives model_search.metric_choices (DEC-039) |
| `entity` | str; noun in copy and messages ("one row per {entity}") |
| `target` | non-UI (plan §5) |
| `target.column` | str \| null; REQUIRED (non-null) when ai_type != generative |
| `target.positive_label` | str \| int \| bool \| null; auto-detected when null |
| `target.definition` | str; Data page "Definition" |
| `target.label_source` | str; Data page "Label source" |
| `primary_key_hints` | list[str]; profile PK detection order (M2) |
| `time_column_hints` | list[str]; time-column detection order (M2) |
| `validation` | data limits + validation switches (plan §4.1, §6.3) |
| `validation.min_rows` | int >= 1; ROWS_TOO_FEW below this (plan §4.1) |
| `validation.max_file_size_mb` | int >= 1; plan §4.1 "Max 2 GB" |
| `validation.imbalance_warn_min_rate` | float 0..1; TARGET_IMBALANCE_SEVERE below this |
| `validation.imbalance_warn_max_rate` | float 0..1; TARGET_IMBALANCE_SEVERE above this |
| `validation.high_null_column_rate` | float 0..1; HIGH_NULL_COLUMN above this |
| `validation.leakage_auc_threshold` | float 0.5..1; single-feature AUC that triggers LEAKAGE_SUSPECTED |
| `validation.acknowledged` | list[str]; run-only, e.g. ["LEAKAGE_SUSPECTED:converted_flag"] (plan §8) |
| `prepare` | [UI 1] Data preparation |
| `split` | [UI 2] Data split |
| `features` | [UI 3] Feature engineering |
| `model_search` | [UI 4] Model search ("metric" renders in UI 5 as "Optimise for") |
| `model_search.metric_choices` | list[enum]; ordered choices of "Optimise for" (DEC-019) |
| `model_search.candidate_pool` | list[enum catalog.model_families]; offered in the grid, in this order |
| `evaluation` | [UI 5] Evaluation & explainability |
| `evaluation.threshold` | DEC-006 |
| `actions` | [UI 6] Actions & output |
| `actions.score_field` | str; score column name in scores.csv |
| `actions.bands` | list[Band]; strictly descending min_score, last must be 0.0, unique names (DEC-008) |
| `actions.suppression.opt_out_column` | str \| null; rows where falsey are suppressed (config-only) |
| `actions.suppression.recently_contacted_column` | str \| null (config-only) |
| `monitoring` | [UI 7] Monitoring & retraining |
| `governance` | [UI 8] Governance & privacy |
| `output` | non-UI (plan §5) |
| `output.kpi.label` | str; headline KPI tile on the Output page |
| `output.kpi.formula` | str; mini-grammar, §4.6 (DEC-007) |
| `ui` | Setup-screen copy (prototype verbatim); {entity} is substituted (DEC-022) |
| `ui.target_label` | str; step-2 label (generative: "Reference answer column") |
| `ui.mode_labels.train` | str |
| `ui.mode_labels.score` | str |
| `ui.run_button.train` | str |
| `ui.run_button.score` | str |
| `ui.pages` | H1 titles of the Data / Model / Output pages (prototype UC d/m/o) |
| `ui.pages.data` | str |
| `ui.pages.model` | str |
| `ui.pages.output` | str |
| `template` | plan §4.3; every use case supplies the full column list |
| `template.columns` | list[TemplateColumn] {name, role, type, description, examples[5]}; §3 |

### Catalog keys

Engine constants. The catalog is never merged into a use case and never overridable (DEC-001).

| Key | Notes |
|---|---|
| `catalog.model_families` | id -> {autogluon_key, label, requires}; candidates/pools must be subsets |
| `catalog.model_families.NeuralNet` | optional "nn" extra (DEC-011) |
| `catalog.strategy_presets` | model_search.strategy -> AutoGluon presets (plan §6.3 train) |
| `catalog.strategy_presets.fast` | str |
| `catalog.strategy_presets.balanced` | str |
| `catalog.strategy_presets.exhaustive` | str |
| `catalog.metrics` | id -> {label, autogluon_name, problem_types, greater_is_better} |
| `catalog.problem_types` | id -> {label (prototype PTYPES verbatim), enabled in Phase 1} |
| `catalog.problem_types.forecasting` | plan §12: later |
| `catalog.problem_types.clustering` | plan §12: later |
| `catalog.ai_types` | id -> {marker on the overview, stars, label} (prototype TYPES verbatim) |
| `catalog.automl_choice` | the step-3 first option; value is a sentinel, never a display string |
| `catalog.automl_choice.value` | str |
| `catalog.automl_choice.label` | str (prototype AUTO) |
| `catalog.column_name_patterns` | case-insensitive regexes used by ingest/profile detection (M2) |
| `catalog.column_name_patterns.time_like` | plan §4.2 verbatim |
| `catalog.column_name_patterns.leakage` | plan §6.3 LEAKAGE_SUSPECTED name patterns churn*, converted*, outcome*, checked on every column |
| `catalog.column_name_patterns.leakage_after_target` | plan §6.3 "*_date after target": checked only on columns placed after the target column in the header (DEC-037) |

## Merge and override rules

### `engine.config.deep_merge`

```
Deep for mappings, replace for everything else; neither argument is mutated (DEC-002).
```

### `engine.config.apply_overrides`

```
Expand, authorise and merge run overrides into a raw config document (config units only).
```
